## 13) Full Todo Coverage Matrix (1:1 from `todo.md`)
This section explicitly carries every current `todo.md` item so nothing is lost.

## Visual polish
- Better `.svg` icons.
  - Target: `Later`.
  - Note: copyright-safe sourcing required.
- Light mode + header toggle (`header_bar.py`).
  - Target: `Next`.
- Header color improvements (`header_bar.py`).
  - Target: `Now`.
- Header button-group background proportion tuning (`header_bar.py`).
  - Target: `Now`.
- Header button hover effect improvement (`header_bar.py`).
  - Target: `Now`.
- Add DTU logo/text from V1 in properties panel (`properties_panel.py`).
  - Target: `Next`.
- Fix status bar white vertical line issue.
  - Target: `Now`.

## Plots
- Update concept HTML with improved plot area design + toolbar/sidebar.
  - Target: `Now` (Phase 1 design-first gate).
- Plot toolbar redesign for compact high-quality controls (`plot_page.py`).
  - Target: `Now`.
  - Status: Done (2026-02-11).
- Plot sidebar and plot-specific options redesign (`plot_sidebar.py` + settings UX).
  - Target: `Now`.
  - Status: Done (2026-02-11).
- Improve overall plot design quality.
  - Target: `Now` (iterative).
- Compass redesign/fix.
  - Target: `Now`.
- Plot resizing robustness fixes.
  - Target: `Now`.
  - Status: Done (2026-02-11).
- Plot settings dialog design/UX overhaul.
  - Target: `Now`.
  - Status: Done (2026-02-11).
- Add subtle in-plot interaction hints.
  - Target: `Now`.

## Statistics panel -> triangle analysis
- KPI pills (total/kept/rejected) consistent size.
  - Target: `Now`.
- Kept/rejected bar quality improvement.
  - Target: `Now`.
- Point rejection frequency + reason breakdown bar quality improvements.
  - Target: `Now`.
- Triangle geometry plot usability/visual improvement.
  - Target: `Now`.
- Table `#` column width issue on large datasets.
  - Target: `Now`.
- Attribute table sync issue after filtering.
  - Target: `Now`.

## Startup and welcome screen (wait)
- Welcome/startup screen inspired by Kornsize.
  - Target: `Later`.
- Splash screen with DTU logo + creator name.
  - Target: `Later`.

## Other things
- Reusable loading/progress dialog for data/calculation/filter workflows.
  - Target: `Next`.
- Performance improvements for large datasets and stats dashboard.
  - Target: `Now` (profiling + obvious fixes), `Next` (deeper work).
- More focus on stacked points.
  - Target: `Now` (diagnostics visibility), `Next` (workflow improvements).

## Gradient analysis features (new ideas)
- Spatial coverage/quality overlay.
  - Target: `Now` (minimum map/inspector layer).
- Sensitivity/robustness (leave-one-out/jackknife).
  - Target: `Next`.
- Outlier diagnostics.
  - Target: `Next`.
- Directional uncertainty/dispersion.
  - Target: `Now`.
- Triangle size awareness (area/edge bins/filters).
  - Target: `Next`.
- Confidence overlay.
  - Target: `Next`.
- Calculation audit trail (settings snapshots in exports/views).
  - Target: `Now`.

## Integration-first brainstorm (kept + rejected)
- Kept-triangle geometry diagnostics parity.
  - Target: `Now`.
- Shared diagnostics model across table/inspector/stats/plots/map.
  - Target: `Now`.
- Synchronized cross-view selection.
  - Target: `Now`.
- Linked filters across views.
  - Target: `Now`.
- Focus mode (point/region local analysis workflow).
  - Target: `Next`.
- Kept vs rejected comparison distributions.
  - Target: `Next`.
- Uncertainty/context badges on key outputs.
  - Target: `Next`.
- Map/inspector legend and semantic parity.
  - Target: `Now`.
- Reproducible exports with state/settings.
  - Target: `Now`.
- Minimal state-transition "why changed" log.
  - Target: `Next`.

